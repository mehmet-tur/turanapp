# Mimari

Bu monorepo, `apps/api`, `apps/web`, `apps/mobile` ve `packages/shared` katmanlarına ayrılır. API tek otoritedir; web ve mobil istemciler yalnızca HTTP sözleşmeleri üzerinden konuşur. Ödeme ve video sağlayıcıları port/adaptor modeliyle soyutlanır.
