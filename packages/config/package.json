{
  "name": "@celebrity-call/config",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'config build not required'",
    "test": "echo 'No config tests yet'",
    "format": "echo 'No config format targets'"
  }
}
